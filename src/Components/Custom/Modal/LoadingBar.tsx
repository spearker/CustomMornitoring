import React from 'react'

interface Props {
  isVisible: boolean
}

const LoadingBar: React.FunctionComponent<Props> = ({ isVisible }) => {
  return (
      <div>

      </div>
  )
}

export default LoadingBar
